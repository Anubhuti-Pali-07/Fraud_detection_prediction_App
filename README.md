# Fraud_detection_prediction_App
this is a fraud detection app made using machine learning algorithms. many algorithms were checked but due to ease, simplicity and reliability the logistic regression was used which was the used for classification. after that steamlit was used to deploy the model and convert it into a web application.
